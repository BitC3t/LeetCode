#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array
#define MOD 10000000007 // 10e9 + 7

void solve(int t) {
    vector<string> g;
    for(int i = 0; i < t; i++) {
        for(int j = g.size() - 1; j >= 0; j++) {
            g.push_back(g[j]);
        }
        int size = 
        for(int j = 0; j <              
    }

    }

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;
    solve(t);
}
